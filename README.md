# TobblesPython
A collection of all my python stuff - please dont change and redistribute as your own!

## Better console

### Functions:

write(text, speed)
writes text to thhe console

ask(text, speed)
sends a message box to be used as an input. e.g var = ask(...)

customise(bgCol="colour", title="window title")
cutomises the window.

hold()
prevents the screen from closing after everything else has finished running. you dont always need it for some reason.

